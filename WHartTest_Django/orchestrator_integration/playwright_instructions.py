"""
UI 自动化用例生成指令

用于 Agent Loop 模式下的 UI 自动化生成任务。
"""

PLAYWRIGHT_SCRIPT_INSTRUCTION = """

## 【强制要求】UI自动化用例生成

## 主要流程
1.判断之前是否生成过UI自动化用例，有则基于存在的用例进行修改和完善，没有则生成新的用例。
2.具体怎么保存用例查看 ui-automation 工具的操作方法和描述。
3.**保存完成后必须自动执行一次该 UI 自动化用例（强制，禁止跳过）**：通过 `execute_skill_script(skill_name="ui-automation", command="python ui_automation_tools.py --action execute_testcase --testcase_id <用例ID> --wait_result --exec_timeout 120")` 执行，并调用 `get_execution_records` 查询执行记录确认已跑完；执行失败按错误分析流程修复后重试一次。只有保存失败或没有在线执行器时，才允许不执行并把原因写入最终 JSON 的 `summary`。

**重要 本次任务必须在执行完所有功能测试步骤后，生成并调用相应工具保存UI自动化用例。**

### 断言规则（非常重要）
1. **禁止猜测 URL**：断言中的 URL 必须使用执行步骤时**实际观察到的 URL**，不要自己编造或猜测
2. **禁止使用通配符模式**：不要使用 `**/dashboard` 这样的模式，必须使用完整的实际 URL
3. **断言必须来源于实际结果**所有断言值 URL、标题、文本等必须是执行过程中**实际看到的值**
4. **当无法确定元素的具体文本时，优先使用可见性断言**
5. **详情页 URL 规则**：验证点击进入的子页面（如详情页）时，若该验证单独保存为页面步骤，其页面 URL 必须使用点击后实际到达的完整 URL（含路径与 ID），禁止使用列表页 URL；更推荐把详情页验证合并进执行点击的同一页面步骤。
6. **录制前固定项目上下文（强制）**：浏览器导航 URL 必须带 `?project=<项目ID>` 查询参数，或先用顶部项目选择器切换到用例所属项目；禁止在错误项目上下文录制步骤，否则项目相关下拉/列表内容错配，执行必失败。
"""
